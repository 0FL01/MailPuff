services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: mailpuff:latest
    container_name: MailPuff
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - TZ=${TZ:-UTC}
    ports:
      - "127.0.0.1:82:8080"